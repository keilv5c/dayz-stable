# 测试者须知 —— Mini DAYZ 联机版

> 这份是给**参与测试的朋友**看的。开发/构建相关内容见 `README-DEV.md`。
> 当前版本：**`mdz-webrtc-web-7-lite`**（面板标题栏右上角也会显示 `mdz-ui-7-lite`，对不上就是装到旧包了）

---

## 一、先说清楚这是什么

- **非官方粉丝作品**，把 Mini DAYZ 改成可以两个人联机，**不收费、不提供游戏本体下载**。
- **零服务器**：两个人直接连，中间没有服务器。所以**没有房间列表**，必须靠线下把二维码/文本给对方。
- 目前定位是**小范围朋友之间互玩（PvE）**，不是公开发布版本。
- 只和**你信得过的人**联机 —— 二维码/SDP 就是准入凭证，没有密码。

---

## 二、装上去

| 平台 | 怎么装 |
|---|---|
| **Android** | 拿到 `Minidayz-WebRTC-debug.apk` 直接点安装（首次要允许"安装未知来源应用"）。首次运行会请求**相机权限**——扫码联机需要它 |
| **iPhone** | 拿到未签名 `.ipa` 后用 Sideloadly + 自己的 Apple ID 签名安装。**注意：免费 Apple ID 签的包只有 7 天有效、最多 3 个 App**，到期要重签 |
| **电脑（浏览器）** | 让出包的人给你网页地址即可（必须 `https://`，否则摄像头被浏览器禁用、扫码模式用不了） |

---

## 三、怎么联机

联机面板在**右上角**（收起后是一个「☰ 联机面板」小按钮，可以拖着移动）。
**游戏里没有扫码图标**——入口就是这个面板。

### 场景一：同一个 Wi-Fi（面对面）—— 推荐

1. 房主：面板 →「① 房主：创建房间」→ 屏幕出现**房间二维码**
2. 客机：面板 →「① 客机：加入房间」→ 用相机扫房主的码 → 出现**回码**
3. 房主：面板 →「② 房主：扫对方回码」→ 扫客机的回码 → 双方状态变「已连接」

> 如果面板提示"只拿到 mDNS 候选"或扫完连不上：房主点「②b 授权摄像头后重出码」，
> 授权一次摄像头权限后重新出码，让客机再扫一次（这样 Offer 里会带真实内网 IP，更稳）。

### 场景二：不在一起（异地）

用「**异地文本 SDP 联机**」，本质是复制粘贴两段文本：

1. 房主点「创建房间」→ 点「复制 Offer」→ 用微信/QQ 发给客机
2. 客机粘到 **Offer 框**里 → 点「② 客机：生成 Answer」→ 点「复制 Answer」→ 发回房主
3. 房主粘到 **Answer 框**里 → 点「③ 房主：确认 Answer 建立连接」

> ⚠️ 异地联机**没有中继服务器**，两边网络"脾气不好"（对称 NAT）时会连不上。
> 连不上不是你的操作问题，换成同一 Wi-Fi 就正常。

### 扫不上码的兜底

二维码只是文本的载体。两种模式下**面板里都有文本握手区**，
二维码浮层里也有「复制握手串（对方扫不上时用文本）」按钮 —— 直接把那段文本发过去粘上即可。

**电脑 ↔ 手机 最稳的走法**：电脑出大二维码 → 手机扫（这条稳）→ 手机点「复制握手串」发回电脑 → 电脑粘贴完成。**只需传一次文本。**

---

## 四、画面被挖孔 / 灵动岛挡住？

面板底部有 **「画面黑边（避开挖孔 / 灵动岛，高度不变）」**：

- 「左」「右」各一条滑杆 + 数字框，**拖动即时生效**（0–400px）
- 快捷按钮：`对称 54` / `对称 80` / `重置黑边`
- **只收左右，高度一点都不动**；设置会记住，下次打开还在

被遮哪边就把哪边调宽。如果拉到最大还是不够，说明那个位置本来就在游戏画面中间，
属于游戏本身的设计问题，黑边帮不了。

---

## 四之二、手机发热、掉帧、卡顿？

面板底部有 **「画面清晰度」**，三档：

| 档位 | 什么时候用 |
|---|---|
| 高（最清晰） | 手机很强、想要最锐利的文字 |
| **中（均衡，默认）** | 一般手机用这个 |
| 低（最省电） | **还是发热/掉帧就切这个** —— 实测能跑满 60 帧 |

**原因**：手机屏幕的像素密度很高，游戏默认会按屏幕密度把画面放大 2~3 倍来渲染 ——
每帧要填的像素数直接是 4~9 倍，但游戏的原始分辨率只有 1024x768，**放大并不会更清晰，纯粹是白烧 GPU**。
填得越多，GPU 越忙，电就变成热。

按手机视口实测（同一份代码，只改渲染倍率）：

| 档位 | 画布像素（中端安卓横屏） | 相对默认 |
|---|---|---|
| 3x（未封顶） | 259 万 | 1x |
| 高 2x | 115 万 | 约 1/2.2 |
| **中 1.5x（默认）** | 65 万 | **约 1/4** |
| 低 1x | 29 万 | **约 1/9** |

调低之后画面会略软一点（像素画本来就看不太出来），但**发热会明显改善**。
设置会记住，下次打开还在。

**怎么知道有没有效果**：面板底部点 **「性能快照」** —— 会测 3 秒，把帧率、帧耗时、
画布像素数、当前渲染倍率打到状态行并自动复制。切档位前后各测一次对比即可。
真机上还可以看手机温度（Android 用 `adb shell dumpsys battery | grep temperature`）。

---

## 五、已知问题（**这些是已知的，不用报**）

本版本按需求删掉了 6 个历史补丁模块，所以下面这些现象是**预期内的**：

1. **客机（不是房主那位）打怪可能不结算伤害**，近战尤其明显。→ 让房主来打，或客机以探索/协作玩法为主。
2. **跨岛会出问题**：客机可能突然拿到房主的角色/背包，而且**卸不下来**。
   → **联机时不要跨岛**。真要跨岛，先断开联机。
3. **长时间高频率交互手机会偏热**（原来的"交互风暴刹车"模块已移除）。
4. **异地连不上**：没有中继服务器，对称 NAT 下必然失败。
5. 单机版（`sp/`）**没有**黑边功能，那是另一条构建线。
6. 主菜单左右两侧原本的 `MDZ☆START` 和 `MINI DayZ 2` 两个入口**已经移除**（视觉和点击都没了）。
   如果你在别的机器上还看得到，说明装的是旧包 —— 看面板标题栏的版本号。

---

## 六、出问题了怎么反馈

面板底部有两个按钮：

- **「复制日志」** —— 点一下把完整日志复制到剪贴板，粘贴发出来即可
- **「连接统计」** —— 把通道状态、ICE 状态、候选数量、收发计数打到状态行（也进日志）

反馈时请带上这三样，能省掉一大半来回：

1. **构建号**：面板标题栏上的 `mdz-ui-…`（或者控制台里 `[MDZ] build …` 那一行）
2. **「复制日志」** 的内容
3. **你们两边分别是什么设备/什么系统**、用的哪种联机方式（同 Wi-Fi 扫码 / 异地文本）

> 看不到面板？按 F12 看控制台：**没有 `[MDZ] build …` 那一行**就说明页面被缓存住了（网页端），
> Ctrl+Shift+R 强制刷新，或用无痕窗口。App 端不会遇到这个问题。

---

## 七、面板上的其它按钮

| 按钮 | 用途 |
|---|---|
| 换个扫码方式（原生 ⇄ 网页） | App 内扫码有两种实现，一种扫不上就换另一种 |
| 显示二维码 | 二维码浮层收起后再打开 |
| 取消 / 断开 | 断开当前联机 |
| 重置面板位置 | 面板挡住游戏按钮时，一键回到右上角（平时直接拖标题栏就能移动） |
