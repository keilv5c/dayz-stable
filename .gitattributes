# ============================================================================
#  .gitattributes —— 保护"按字节敏感"的文件，禁止 git 自动改行尾
# ----------------------------------------------------------------------------
#  背景：
#    本机 git 的 core.autocrlf=true，会做 LF<->CRLF 转换。这对源码无妨，
#    但对下面这些文件是**破坏性**的：
#
#      · sp/web/*.xml        —— 文本里有大量由 U+0336（组合长横线）拼成的
#                               整行"分隔线"，行结构敏感；而且这些 XML 是要
#                               喂给游戏引擎解析的，最好和发布形态逐字节一致。
#      · sp/web/data.js      —— 12 MB 的压缩 JSON，唯一的游戏逻辑载体。
#      · sp/web/*.js / *.png / *.ogg / *.m4a
#                            —— 游戏运行资源，不该被 git 碰。
#      · *.ps1               —— PowerShell 5.1 对"无 BOM"文件的解码很挑，
#                               且我们不希望 checkout 时被塞进 CR 或改 BOM。
#
#  做法：把这些路径标记成 -text（git 视为二进制，不做任何行尾转换），
#        checkout 出来的就是提交进去的原样字节。
#
#  注意：`-text` 也会关掉 diff 的文本高亮，这是刻意的取舍 —— 正确性优先。
# ============================================================================

# ---- 游戏资源：整体按二进制处理，绝不转换 ----
sp/web/**            -text
web/**               -text

# ---- iOS 工程与构建脚本 ----
sp/ios/**            -text
*.ps1                -text

# ---- 其余按正常文本处理（让 git 自己决定）----
*.md                 text
*.yml                text
*.json               text
*.ts                 text
*.rb                 text
Podfile              text
.gitignore           text
.gitattributes       text
